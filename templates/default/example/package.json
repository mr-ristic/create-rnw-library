{
  "name": "{{name}}-example",
  "homepage": ".",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@types/react": "{{#if yarn}}link:..{{else}}file:..{{/if}}/node_modules/@types/react",
    "@types/react-dom": "^17.0.11",
    "react": "{{#if yarn}}link:..{{else}}file:..{{/if}}/node_modules/react",
    "react-dom": "{{#if yarn}}link:..{{else}}file:..{{/if}}/node_modules/react-dom",
    "react-scripts": "^4.0.3",
    "{{name}}": "{{#if yarn}}link:..{{else}}file:..{{/if}}"
  },
  "devDependencies": {
    "@babel/plugin-syntax-object-rest-spread": "^7.8.3",
    "typescript": "^4.5.2"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
